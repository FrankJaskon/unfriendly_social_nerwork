export const getResponseWarning = ({app: {responseWarning}}) => {
    return responseWarning;
}

export const getInitialized = ({app: {initialized}}) => {
    return initialized;
}

export const getLoadingError = ({app: {loadingError}}) => {
    return loadingError;
}
